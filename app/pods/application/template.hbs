<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      {{link-to 'Fido' 'index' class="navbar-brand"}}
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        {{#if session.isAuthenticated}}
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Contacts <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li>{{link-to 'View' 'contacts.index'}}</li>
              <li>{{link-to 'Create' 'contacts.new'}}</li>
            </ul>
          </li>
        {{/if}}
      </ul>

      <ul class="nav navbar-nav navbar-right">
        {{#if session.isAuthenticated}}
          <li>{{link-to 'Settings' 'settings'}}</li>
          <li><a href="#" {{action 'invalidateSession'}}>Logout</a></li>
        {{else}}
          <li>{{link-to 'Login' 'login'}}</li>
          <li>{{link-to 'Signup' 'signup'}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<div class="container">
  {{outlet}}
</div>
